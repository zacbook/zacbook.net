knitr::opts_chunk$set(engine.path = list(python = '/usr/local/bin/python3'))
options(
    blogdown.author='Zac Harmany',
    blogdown.ext='.Rmd',
    blogdown.subdir='post',
    blogdown.yaml.empty=TRUE,
    servr.daemon=interactive()
)
# Rprofile needs a trailing newline
